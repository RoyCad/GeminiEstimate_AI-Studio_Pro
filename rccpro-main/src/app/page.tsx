export default function Home() {
  // This page is no longer used for redirection.
  // The redirection from "/" to "/login" is now handled in next.config.js
  // This component is kept to avoid Next.js errors about a missing root page.
  return null;
}
